import React, { useEffect, useState } from 'react';
import { auth } from '../../lib/firebase';
import { AlertTriangle, Building2, CalendarClock, CheckCircle2, RefreshCw, UserPlus } from 'lucide-react';
import { ClientPortalConfiguration } from './ClientPortalConfiguration';

type RequestRecord = Record<string, any> & { id: string; requestNumber: string; companyName: string; siteName: string; status: string; requestedWindows: Array<{ date: string; start: string; end: string }> };
type ContractorOption = { id: string; name: string; email: string };

async function adminApi(action: string, options: RequestInit = {}) {
  const token = await auth.currentUser?.getIdToken();
  const response = await fetch(`/api/admin/client-portal?action=${action}`, { ...options, headers: { Authorization: `Bearer ${token}`, ...(options.body ? { 'Content-Type': 'application/json' } : {}), ...(options.headers || {}) } });
  const data = await response.json();
  if (!response.ok) throw new Error(data.error || 'Admin request failed.');
  return data;
}

export function ClientRequestsAdmin({ contractors }: { contractors: ContractorOption[] }) {
  const [data, setData] = useState<any>({ requests: [], organizations: [], users: [], appointments: [], failedNotifications: [] });
  const [loading, setLoading] = useState(true);
  const [notice, setNotice] = useState('');
  const [convert, setConvert] = useState({ requestId: '', technicianLeadId: '', hourlyRate: '55', travelRate: '35', directContactApproved: false });
  const [schedule, setSchedule] = useState({ appointmentId: '', start: '', end: '', technicianId: '' });
  const [org, setOrg] = useState({ name: '', approvedDomains: '', referencePrefixes: '', billingEmail: '', defaultContactPolicy: 'techsavvy_only' });
  const load = async () => { setLoading(true); try { setData(await adminApi('dashboard')); } catch (error) { setNotice(error instanceof Error ? error.message : 'Could not load requests.'); } finally { setLoading(false); } };
  useEffect(() => { void load(); }, []);
  const post = async (action: string, body: unknown) => { setNotice(''); try { await adminApi(action, { method: 'POST', body: JSON.stringify(body) }); await load(); setNotice('Saved successfully.'); } catch (error) { setNotice(error instanceof Error ? error.message : 'Could not save.'); } };
  if (loading) return <div className="grid min-h-64 place-items-center text-sm text-slate-400"><RefreshCw className="h-5 w-5 animate-spin" /></div>;
  const pendingUsers = data.users.filter((user: any) => user.status !== 'active');
  const openRequests = data.requests.filter((request: RequestRecord) => !['declined'].includes(request.status));
  return <div className="space-y-6">
    {notice && <div className="rounded border border-amber-500/30 bg-amber-500/10 p-3 text-xs text-amber-200">{notice}</div>}
    {data.failedNotifications.length > 0 && <div className="flex gap-3 rounded-xl border border-red-500/30 bg-red-500/10 p-4 text-sm text-red-200"><AlertTriangle className="h-5 w-5 shrink-0" /><div><strong>{data.failedNotifications.length} notification deliveries need attention.</strong><p className="mt-1 text-xs text-red-200/70">Review provider configuration or delivery errors before relying on alerts.</p></div></div>}
    <div className="grid gap-4 md:grid-cols-4">{[['New requests',data.requests.filter((r:RequestRecord)=>r.status==='requested').length],['Needs clarification',data.requests.filter((r:RequestRecord)=>r.status==='clarification_needed').length],['Pending users',pendingUsers.length],['Appointments',data.appointments.length]].map(([label,value])=><div key={String(label)} className="rounded-xl border border-slate-800 bg-slate-950 p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-slate-500">{label}</p><p className="mt-2 text-2xl font-black text-white">{value}</p></div>)}</div>
    <section><h3 className="mb-3 text-sm font-bold text-white">Job requests</h3><div className="space-y-3">{openRequests.map((request:RequestRecord)=><article key={request.id} className="rounded-xl border border-slate-800 bg-slate-950 p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex items-center gap-2"><span className="font-mono text-[10px] text-green-400">{request.requestNumber}</span>{request.urgent&&<span className="rounded bg-red-500/15 px-2 py-0.5 text-[9px] font-bold text-red-300">URGENT</span>}</div><h4 className="mt-1 text-lg font-bold text-white">{request.siteName}</h4><p className="text-xs text-slate-400">{request.companyName} · {request.clientReference} · {request.requesterName}</p><p className="mt-2 text-xs text-slate-300">{request.scopeSummary}</p><p className="mt-2 text-[10px] text-slate-500">Preferred: {request.requestedWindows?.[0]?.date} {request.requestedWindows?.[0]?.start}–{request.requestedWindows?.[0]?.end}</p></div><span className="rounded bg-white/5 px-3 py-1 text-xs capitalize text-amber-300">{request.status.replace(/_/g,' ')}</span></div><div className="mt-4 flex flex-wrap gap-2"><button onClick={()=>post('request-status',{requestId:request.id,status:'reviewing'})} className="rounded border border-slate-700 px-3 py-2 text-[10px] font-bold text-slate-300">Reviewing</button><button onClick={()=>{const note=window.prompt('What clarification is needed?');if(note)void post('request-status',{requestId:request.id,status:'clarification_needed',reviewNote:note})}} className="rounded border border-amber-500/30 px-3 py-2 text-[10px] font-bold text-amber-300">Request clarification</button><button onClick={()=>setConvert((v)=>({...v,requestId:request.id}))} disabled={Boolean(request.convertedJobId)} className="rounded bg-green-500 px-3 py-2 text-[10px] font-bold text-slate-950 disabled:opacity-40">{request.convertedJobId?'Converted':'Convert to work order'}</button><button onClick={()=>{const note=window.prompt('Reason for declining');if(note)void post('request-status',{requestId:request.id,status:'declined',reviewNote:note})}} className="rounded border border-red-500/30 px-3 py-2 text-[10px] font-bold text-red-300">Decline</button></div>{convert.requestId===request.id&&<div className="mt-4 grid gap-2 rounded border border-green-500/20 bg-green-500/5 p-4 md:grid-cols-4"><select value={convert.technicianLeadId} onChange={(e)=>setConvert((v)=>({...v,technicianLeadId:e.target.value}))} className="rounded bg-slate-900 p-2 text-xs text-white"><option value="">Choose technician</option>{contractors.map((contractor)=><option key={contractor.id} value={contractor.id}>{contractor.name}</option>)}</select><input type="number" value={convert.hourlyRate} onChange={(e)=>setConvert((v)=>({...v,hourlyRate:e.target.value}))} className="rounded bg-slate-900 p-2 text-xs text-white" placeholder="Hourly rate"/><input type="number" value={convert.travelRate} onChange={(e)=>setConvert((v)=>({...v,travelRate:e.target.value}))} className="rounded bg-slate-900 p-2 text-xs text-white" placeholder="Travel rate"/><button onClick={()=>post('convert',{...convert,assignedTechIds:convert.technicianLeadId?[convert.technicianLeadId]:['ALL']})} className="rounded bg-green-500 p-2 text-xs font-bold text-slate-950">Create work order</button><label className="md:col-span-4 flex gap-2 text-xs text-slate-300"><input type="checkbox" checked={convert.directContactApproved} onChange={(e)=>setConvert((v)=>({...v,directContactApproved:e.target.checked}))}/>Approve direct technician contact for this job</label></div>}</article>)}</div></section>
    <section><h3 className="mb-3 flex items-center gap-2 text-sm font-bold text-white"><CalendarClock className="h-4 w-4 text-green-400"/> Scheduling</h3><div className="grid gap-3 lg:grid-cols-2">{data.appointments.map((appointment:any)=><article key={appointment.id} className="rounded-xl border border-slate-800 bg-slate-950 p-4"><p className="text-xs font-bold text-white">Appointment {appointment.id.slice(-6)}</p><p className="mt-1 text-[10px] capitalize text-slate-500">{appointment.status}</p>{appointment.confirmedStart&&<p className="mt-2 text-xs text-green-300">{new Date(appointment.confirmedStart).toLocaleString()}</p>}<button onClick={()=>setSchedule((v)=>({...v,appointmentId:appointment.id,technicianId:appointment.technicianId||''}))} className="mt-3 text-[10px] font-bold text-green-400">Confirm / change schedule</button>{schedule.appointmentId===appointment.id&&<div className="mt-3 grid grid-cols-2 gap-2"><input type="datetime-local" value={schedule.start} onChange={(e)=>setSchedule((v)=>({...v,start:e.target.value}))} className="rounded bg-slate-900 p-2 text-[10px] text-white"/><input type="datetime-local" value={schedule.end} onChange={(e)=>setSchedule((v)=>({...v,end:e.target.value}))} className="rounded bg-slate-900 p-2 text-[10px] text-white"/><select value={schedule.technicianId} onChange={(e)=>setSchedule((v)=>({...v,technicianId:e.target.value}))} className="rounded bg-slate-900 p-2 text-[10px] text-white"><option value="">Technician</option>{contractors.map((c)=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button onClick={()=>post('schedule',{appointmentId:appointment.id,start:new Date(schedule.start).toISOString(),end:new Date(schedule.end).toISOString(),technicianId:schedule.technicianId})} className="rounded bg-amber-500 p-2 text-[10px] font-bold text-slate-950">Confirm</button></div>}</article>)}</div></section>
    <div className="grid gap-6 lg:grid-cols-2"><section className="rounded-xl border border-slate-800 bg-slate-950 p-5"><h3 className="mb-4 flex items-center gap-2 text-sm font-bold text-white"><UserPlus className="h-4 w-4 text-green-400"/>Pending memberships</h3>{pendingUsers.length===0?<p className="text-xs text-slate-500">No pending memberships.</p>:pendingUsers.map((user:any)=><div key={user.id} className="mb-3 flex items-center justify-between gap-3 rounded bg-slate-900 p-3"><div><p className="text-xs font-bold text-white">{user.displayName}</p><p className="text-[10px] text-slate-500">{user.email} · email {user.emailVerified?'✓':'—'} · phone {user.phoneVerified?'✓':'—'}</p></div><button disabled={!user.emailVerified||!user.phoneVerified} onClick={()=>post('approve-member',{uid:user.id,roles:user.requestedRoles})} className="rounded bg-green-500 px-3 py-1.5 text-[10px] font-bold text-slate-950 disabled:opacity-30">Approve</button></div>)}</section><section className="rounded-xl border border-slate-800 bg-slate-950 p-5"><h3 className="mb-4 flex items-center gap-2 text-sm font-bold text-white"><Building2 className="h-4 w-4 text-green-400"/>Add client company</h3><div className="grid gap-2"><input value={org.name} onChange={(e)=>setOrg((v)=>({...v,name:e.target.value}))} placeholder="Company name" className="rounded bg-slate-900 p-2 text-xs text-white"/><input value={org.approvedDomains} onChange={(e)=>setOrg((v)=>({...v,approvedDomains:e.target.value}))} placeholder="Domains, comma separated" className="rounded bg-slate-900 p-2 text-xs text-white"/><input value={org.referencePrefixes} onChange={(e)=>setOrg((v)=>({...v,referencePrefixes:e.target.value}))} placeholder="Prefixes, comma separated" className="rounded bg-slate-900 p-2 text-xs text-white"/><input value={org.billingEmail} onChange={(e)=>setOrg((v)=>({...v,billingEmail:e.target.value}))} placeholder="Billing email" className="rounded bg-slate-900 p-2 text-xs text-white"/><button onClick={()=>post('organization',{...org,approvedDomains:org.approvedDomains.split(','),referencePrefixes:org.referencePrefixes.split(',')})} className="mt-1 rounded bg-green-500 p-2 text-xs font-bold text-slate-950">Save company</button></div></section></div>
    <ClientPortalConfiguration data={data} contractors={contractors} post={post} />
  </div>;
}
