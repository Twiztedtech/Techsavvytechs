import React from 'react';

type PreviewJob = {
  id: string;
  name: string;
  address: string;
  notes?: string;
  hourlyRate?: number;
  workOrderNumber?: string;
  vendorName?: string;
  siteContact?: string;
  targetCompletion?: string;
  equipment?: Array<{ description: string; quantity?: string; notes?: string; fulfillmentSource?: string }>;
  scopeTasks?: string[];
  qaChecklist?: string[];
  attachments?: Array<{ name: string; url: string }>;
  signedWorkOrders?: Array<{ id: string; fileName: string; url: string }>;
};

export function TechnicianWorkOrderPreview({ job, technicianName, onClose }: { job: PreviewJob; technicianName: string; onClose: () => void }) {
  const equipment = job.equipment?.filter((item) => item.description.trim()) ?? [];
  const scope = job.scopeTasks?.filter(Boolean) ?? [];
  const checklist = job.qaChecklist?.filter(Boolean) ?? [];
  return <div className="fixed inset-0 z-[1000] flex items-start justify-center overflow-y-auto bg-slate-950/85 p-3 sm:p-6"><div className="my-4 w-full max-w-md overflow-hidden rounded-2xl border border-green-500/40 bg-slate-950 shadow-2xl"><div className="sticky top-0 z-10 border-b border-green-500/25 bg-slate-950 px-5 py-4"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-green-400">Technician preview · read only</p><h2 className="mt-1 text-lg font-black text-white">{job.name}</h2><p className="mt-1 text-xs text-slate-400">Viewing as {technicianName}</p></div><button type="button" onClick={onClose} className="rounded border border-slate-700 bg-slate-900 px-3 py-1.5 text-xs font-bold text-slate-200 hover:border-green-500 hover:text-white" aria-label="Close preview">Close ✕</button></div></div><div className="space-y-3 p-4"><div className="rounded-lg border border-green-500/30 bg-green-500/5 p-3 text-xs space-y-2"><div className="flex items-center justify-between gap-3"><span className="font-bold uppercase tracking-wider text-[10px] text-green-400">Field Service Work Order</span><span className="font-mono text-[10px] text-slate-400">{job.workOrderNumber || job.id}</span></div><div className="grid grid-cols-2 gap-x-4 gap-y-1 text-slate-300"><span className="text-slate-500">Vendor</span><span>{job.vendorName || 'Not specified'}</span><span className="text-slate-500">Site contact</span><span>{job.siteContact || 'Not specified'}</span><span className="text-slate-500">Target completion</span><span>{job.targetCompletion || 'Not specified'}</span><span className="text-slate-500">Agreed rate</span><span className="font-mono font-bold text-green-400">${Number(job.hourlyRate ?? 55).toFixed(2)}/hr</span></div></div><div className="rounded-lg border border-green-500/20 bg-slate-900/40 p-3 text-xs"><div className="flex items-center justify-between gap-3"><div><span className="block font-semibold uppercase tracking-wider text-[10px] text-green-400">On-site time clock</span><p className="mt-1 text-slate-400">Technician starts at arrival and stops when leaving. Each calendar day is saved as its own entry.</p></div><button type="button" disabled className="shrink-0 rounded bg-green-500/25 px-3 py-2 text-[10px] font-bold text-green-200">Start shift</button></div></div><div className="rounded-lg border border-slate-800 bg-slate-900/40 p-3 text-xs"><span className="block font-semibold uppercase tracking-wider text-[10px] text-slate-400">Address</span><p className="mt-1 text-slate-200">📍 {job.address}</p></div><div className="rounded-lg border border-slate-800 bg-slate-900/40 p-3 text-xs"><span className="block font-semibold uppercase tracking-wider text-[10px] text-slate-400">Site instructions / notes</span>{scope.length ? <ol className="mt-2 list-decimal space-y-1 pl-4 leading-relaxed text-slate-200">{scope.map((task, index) => <li key={`${index}-${task}`}>{task}</li>)}</ol> : <p className="mt-1 leading-relaxed text-slate-200">{job.notes || 'No special instructions recorded.'}</p>}</div>{equipment.length ? <div className="rounded-lg border border-slate-800 bg-slate-900/40 p-3 text-xs"><span className="block font-semibold uppercase tracking-wider text-[10px] text-slate-400">Equipment & materials</span><ul className="mt-2 space-y-1 text-slate-200">{equipment.map((item, index) => <li key={`${index}-${item.description}`}><strong>{item.quantity ? `${item.quantity} × ` : ''}{item.description}</strong><span className="ml-2 rounded bg-green-500/10 px-1.5 py-0.5 text-[9px] text-green-300">{item.fulfillmentSource === 'techsavvy_supplied' ? 'TechSavvy supplied' : 'Customer shipped'}</span>{item.notes ? ` — ${item.notes}` : ''}</li>)}</ul></div> : null}{checklist.length ? <div className="rounded-lg border border-slate-800 bg-slate-900/40 p-3 text-xs"><span className="block font-semibold uppercase tracking-wider text-[10px] text-slate-400">Customer sign-off checklist</span><ul className="mt-2 space-y-1 text-slate-200">{checklist.map((item, index) => <li key={`${index}-${item}`}>□ {item}</li>)}</ul></div> : null}<div className="rounded-lg border border-slate-800 bg-slate-900/40 p-3 text-xs space-y-2"><span className="block font-semibold uppercase tracking-wider text-[10px] text-slate-400">Work order documents</span>{job.attachments?.length ? job.attachments.map((file) => <a key={file.url} href={file.url} target="_blank" rel="noopener noreferrer" className="flex items-center justify-between gap-3 rounded border border-slate-700 px-2.5 py-2 text-slate-200 hover:border-green-500 hover:text-green-300"><span className="truncate">📎 {file.name}</span><span className="shrink-0 text-[10px]">Open ↗</span></a>) : <p className="text-slate-500">No documents attached to this work order.</p>}</div>{job.signedWorkOrders?.length ? <div className="rounded-lg border border-green-500/20 bg-slate-900/40 p-3 text-xs space-y-2"><span className="block font-semibold uppercase tracking-wider text-[10px] text-green-400">Signed work orders</span>{job.signedWorkOrders.map((file) => <a key={file.id} href={file.url} target="_blank" rel="noopener noreferrer" className="flex items-center justify-between gap-3 rounded border border-slate-700 px-2.5 py-2 text-slate-200 hover:border-green-500 hover:text-green-300"><span className="truncate">✓ {file.fileName}</span><span className="shrink-0 text-[10px]">Open ↗</span></a>)}</div> : null}<button type="button" disabled className="w-full rounded bg-green-500/25 px-3 py-2 text-[11px] font-bold text-green-200 opacity-80">Complete & Sign Work Order <span className="font-normal">(available to the technician)</span></button></div></div></div>;
}
